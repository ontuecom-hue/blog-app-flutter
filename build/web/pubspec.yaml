name: blog_app_flutter
description: Simple Flutter + Supabase blog starter
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  supabase_flutter: ^2.5.6
  flutter_riverpod: ^2.5.1
  go_router: ^13.2.0
  image_picker: ^1.0.7
  cached_network_image: ^3.3.1

flutter:
  uses-material-design: true
